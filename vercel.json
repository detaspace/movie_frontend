{
  "version": 2,
  "public": true,
  "routes": [
    // Deep Link URL /s/(.*) को index.html पर रूट करें
    {
      "src": "/s/(.*)",
      "dest": "/index.html"
    },
    // Fallback: सभी अन्य URLs को index.html पर भेजें
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
